import initialState from './seoMenu-state';

const seoMenuReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default seoMenuReducer;
