import initialState from './knowMore-state';

const knowMoreReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default knowMoreReducer;
