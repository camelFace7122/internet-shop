import initialState from './footer-state';

const footerReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default footerReducer;
