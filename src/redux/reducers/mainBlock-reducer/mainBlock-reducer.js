import initialState from './mainBlock-state';

const mainBlockReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default mainBlockReducer;
