import initialState from './wideCarousel-state';

const wideCarouselReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default wideCarouselReducer;
